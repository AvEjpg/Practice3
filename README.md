# Система учета заявок сервисного центра

Веб-приложение для автоматизации учета заявок на ремонт техники с разграничением прав доступа по ролям, QR-кодами для обратной связи и аналитикой.

## 📁 Структура проекта

```
Pracrice3/
├── backend/                    # FastAPI бэкенд (REST API)
│   ├── auth.py                # Аутентификация, JWT, авторизация по ролям
│   ├── crud.py                # CRUD операции с базой данных
│   ├── database.py            # Подключение к PostgreSQL
│   ├── main.py                # Основное FastAPI приложение
│   ├── models.py              # SQLAlchemy модели (User, Request, Comment)
│   ├── schemas.py             # Pydantic схемы для валидации
│   └── routers/               # Маршруты API
│       ├── auth.py           # Логин, получение токена
│       ├── client.py         # API для клиентов (заказчиков)
│       ├── comments.py       # Комментарии к заявкам
│       ├── qr.py             # Генерация QR-кодов
│       ├── requests.py       # Управление заявками
│       └── users.py          # Управление пользователями
│
├── frontend/                  # Flask фронтенд (веб-интерфейс)
│   ├── app.py                # Основное Flask приложение
│   └── templates/            # HTML шаблоны

│
└── README.md                # Документация
```

## 🚀 Быстрый старт

### 1. Предварительные требования

- Python 3.8+
- PostgreSQL 12+
- pip (менеджер пакетов Python)

### 2. Установка зависимостей

```bash
# Клонирование проекта (если нужно)
# git clone <repository-url>
# cd service-center-system

# Установка зависимостей бэкенда
cd backend
pip install -r requirements.txt

# Установка зависимостей фронтенда
cd ../frontend
pip install -r requirements.txt
```

### 3. Настройка базы данных

```bash
# Создание базы данных в PostgreSQL
createdb service_center_system

# Или через SQL
CREATE DATABASE service_center_system;
```

### 4. Конфигурация подключения к БД

Отредактируйте файл `backend/database.py` при необходимости:
```python
DATABASE_URL = "postgresql://postgres:123@localhost:5432/service_center_system"
# Формат: postgresql://username:password@host:port/database_name
```

### 5. Запуск приложения

```bash
# Терминал 1: Запуск бэкенда (FastAPI)
cd backend
uvicorn main:app --reload --host 0.0.0.0 --port 8000

# Терминал 2: Запуск фронтенда (Flask)
cd frontend
python app.py
```

### 6. Доступ к приложению

- **Веб-интерфейс (фронтенд)**: http://localhost:5000
- **API (бэкенд)**: http://localhost:8000
- **Автоматическая документация API (Swagger)**: http://localhost:8000/docs
- **Альтернативная документация API (ReDoc)**: http://localhost:8000/redoc

## 👥 Тестовые пользователи

В системе предусмотрено 5 ролей с разными правами доступа:

Логин	Пароль	Роль
kasoo	root	Менеджер
test1	test1	Мастер
login2	pass2	Заказчик
quality_manager	quality123	Менеджер по качеству


## 🛠 Технологии

- **Backend**: FastAPI, SQLAlchemy, PostgreSQL, JWT (PyJWT)
- **Frontend**: Flask, Bootstrap 5, JavaScript
- **База данных**: PostgreSQL с схемой `service_center`
- **Аутентификация**: JWT токены с сроком жизни 24 часа
- **Деплой**: Uvicorn (ASGI сервер)

## 📊 Функциональность

### 1. **Управление заявками**
- Создание, просмотр, редактирование, удаление заявок
- Назначение мастеров на заявки
- Изменение статусов
- Продление сроков выполнения
- Поиск и фильтрация заявок

### 2. **Разграничение прав доступа**
- **Заказчик**: только свои заявки, создание новых
- **Оператор**: регистрация заявок, просмотр всех
- **Мастер**: работа с заявками, комментарии
- **Менеджер**: полный доступ + управление пользователями
- **Менеджер по качеству**: контроль качества, назначение мастеров

### 3. **Аналитика и статистика**
- Количество заявок (всего/выполнено)
- Среднее время выполнения
- Распределение по типам оборудования
- Визуализация данных

### 4. **QR-код обратной связи**
- Генерация QR-кода для опроса качества
- Ссылка на Google Forms для сбора отзывов
- Доступно всем авторизованным пользователям

### 5. **Комментарии к заявкам**
- Общение между специалистами и клиентами
- История работ по заявке
- Привязка к конкретной заявке и мастеру

### 6. **Управление пользователями**
- Создание новых пользователей (только менеджер)
- Редактирование данных (менеджер и менеджер по качеству)
- Удаление пользователей (только менеджер)
- Просмотр списка пользователей

